# Intake Source of Truth Matrix — Program Intake Sprint 001 (2026-08-04)

Phase 2 requirement: for each phase of the intake journey, who is the canonical owner? Two owners for the
same fact = defect. Full narrative: `INTAKE_ARCHITECTURE_REPORT.md`.

| Concept | Canonical owner | Second signal exists? | Verdict |
|---|---|---|---|
| **Upload** (the file arrived) | `predmet_dokumenti` row existing (Pipeline A/C) or `intake_jobs` row (Pipeline B) | No — one row per pipeline, no collision | Clean |
| **OCR result (text)** | Not persisted anywhere as a durable artifact on any pipeline — lives only in-memory for the duration of one request/job, then discarded (only the truncated `tekst_sadrzaj` and Pinecone chunks survive) | N/A | **Gap, not a duplication** — nothing to reconstruct-from if a later step needs the original text again |
| **OCR confidence** | `intake_documents.ocr_confidence` (Pipeline B only) | Pipeline A/C hardcode `0.6` when `ocr_used=True` (Program Beta finding, unchanged) — not a second *source*, a missing *real* measurement on 2 of 3 pipelines | Not a two-owner conflict; a known coverage gap |
| **Classification (`document_type`/`tip_dokaza`)** | `predmet_dokumenti.tip_dokaza`, last-writer-wins between `intake_worker.py` (English vocab, synchronous) and `evidence.py::klasifikuj_i_sacuvaj` (Serbian vocab, unawaited background task) | **Yes — the already-documented Gamma Fork E race, re-confirmed live unchanged** | **Defect, pre-existing, not introduced or fixed this sprint** — `ALPHA-003`/Gamma Fork E |
| **Document `status`** | `predmet_dokumenti.status` — before this sprint, 2 of 5 writers left it at the misleading `na_cekanju` DB default; fixed this sprint (§ below) | `intake_jobs.status` (Pipeline B, richer 9-value enum) answers a *different* question (queue/worker state, not case-file document state) and is discarded at finalize, not merged with the case-file status | **3-way fragmented by design, not accidental collision** — `predmet_dokumenti.status`, `tip_dokaza`/`klasifikovan_at`, `intake_jobs.status` each answer a genuinely different question for a genuinely different audience (case file view / evidence classification / worker queue). Not eliminated this sprint (`INTAKE-003`/`INTAKE-004`) but the specific "silently defaults to a lie" defect within `predmet_dokumenti.status` itself IS fixed. |
| **Processing status** (is the async job still running) | `intake_jobs.status` (Pipeline B only — Pipeline A/C have no async processing status, they're synchronous) | No | Clean, single owner, scoped correctly |
| **Retry status / attempt count** | `intake_jobs.attempts`/`max_attempts` (Pipeline B `intake_queue.py`) | Pipeline A/C have no retry mechanism at all (synchronous, one attempt) | Clean — retry is a Pipeline-B-only concept, correctly scoped, not duplicated |
| **"Is this job actually, fully done"** | Before this sprint: ambiguous — `intake_documents` row existing was treated as sufficient. **Fixed this sprint**: `intake_processing_outcomes` having a row is now the single required signal (`has_processing_outcome()`), since it's the true last-write in both success branches of `_process()` | N/A after fix | **Was the sprint's most severe defect — now single-owner, correctly** |
| **Original file bytes** | Pipeline B/C: `intake-dokumenti` Storage bucket (AES-GCM encrypted), `storage_path` column points to a real key. Pipeline A: **fixed this sprint** — same bucket/encryption scheme, best-effort | No second copy exists anywhere on any pipeline (Pinecone holds only chunked/derived text, never raw bytes) | Clean, single owner, now consistent across all 3 pipelines |
| **Pinecone namespace** | `shared/kancelarija_utils.py::rag_owner_namespace(user_id, kancelarija_id)` — confirmed identical at every write site (`api.py`, `smart_intake.py`, `drafting.py`) | No | **Clean, positively confirmed, no drift found** |
| **Correlation ID** | Middleware-minted at `api.py::correlation_id_middleware`, inherited via `asyncio.create_task`'s context-copy into every background task on the reachable Pipeline A journey | `_require_auth`'s internal `set_request_context()` call is dead/inert (currently harmless only because middleware runs first — `ALPHA-001`, pre-existing, latent) | Clean today, fragile if reordered — documented, not touched (out of this sprint's narrow bound) |
| **Audit trail** | `audit_immutable` table via `log_action()` — before this sprint, only 2 call sites existed on the entire Pipeline A journey (`dokument_upload`, `evidence_klasifikacija`); **`dokument_view` added this sprint** | Pipeline B/C's `intake_audit_log` table (migration 073) is a **separate, parallel** audit mechanism with no `correlation_id` column and no cross-reference to `audit_immutable` | **Two disconnected audit systems, not merged this sprint** — a real architectural gap (`INTAKE-004`-adjacent), documented not fixed; each pipeline's audit trail is internally consistent, just not unified across pipelines |
| **Provenance** (`case_context()` wrapping) | Only the 2 AI-calling steps per pipeline (classification, procena/hronologija/metapodaci) are wrapped; OCR, chunk/embed, DB insert have none | No competing signal, just absent | Coverage gap, not a conflict — consistent with, not duplicating, Program Beta's separate OCR-confidence finding |

**Summary verdict**: zero *genuine* two-owner collisions were found for the same fact answered two
different, contradictory ways — except the pre-existing classification race (Gamma Fork E, unchanged) and
the now-fixed "is the job done" ambiguity. The apparent 3-way status fragmentation is real but is
different-questions-different-tables, not the same-question-two-answers shape the mission's charter calls a
defect; it is nonetheless tracked (`INTAKE-003`/`INTAKE-004`) because the *boundary* between those tables is
undocumented anywhere else in the codebase before this matrix.
