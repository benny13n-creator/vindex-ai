# Phase 2.3.1 Hotfix — Doc Confidence Gate Bias
**Datum:** 2026-05-11  
**Grana:** `feature/p2-3-1-doc-gate-bias`  
**Testovi:** 53/53 prošli (5 novih + 48 postojećih)  
**30Q baseline:** 20✅/8⚠️/2❌ — identično pre-hotfix stanju

---

## Problem

Faza 2.3 je prikazivala LOW refusal pri pitanjima o upload-ovanim dokumentima, čak i kada dokument sadrži direktan odgovor. Uzrok: confidence gate u `ask_agent` uzima u obzir samo `top_score` iz zakon pretrage, ne uzimajući u obzir score-ove iz `doc_passages` (tmp_ namespace).

---

## Popravka (`main.py`, u `ask_agent`)

Odmah nakon `retrieve_documents(...)` call-a, a pre bilo kakvog confidence band grananja, dodato je:

```python
if extra_namespaces:
    doc_passages = retrieval_meta.get("doc_passages", [])
    if doc_passages:
        top_doc_score = max((p.get("score", 0.0) for p in doc_passages), default=0.0)
        if top_doc_score >= 0.5:
            band_up = {"LOW": "MEDIUM", "MEDIUM": "HIGH", "HIGH": "HIGH"}
            old_band = retrieval_meta.get("confidence", "LOW")
            new_band = band_up.get(old_band, old_band)
            if new_band != old_band:
                retrieval_meta["confidence"] = new_band
                logger.info("[DOC_GATE_BIAS] doc_top=%.3f law_top=%.3f band %s→%s", ...)
```

**Threshold:** doc `score >= 0.5` (Pinecone cosine similarity)  
**Band mapiranje:** LOW→MEDIUM, MEDIUM→HIGH, HIGH→HIGH (idempotent)  
**Uslov:** aktivan samo kada `extra_namespaces` postoji → 30Q nije pogođen

---

## Testovi (`tests/test_ask_agent_gate_bias.py`)

| Test | Opis | Rezultat |
|------|------|----------|
| test_gate_bias_low_to_medium | doc score 0.7, zakon LOW → MEDIUM, LLM pozvan | ✅ |
| test_gate_bias_medium_to_high | doc score 0.8, zakon MEDIUM → HIGH, LLM pozvan | ✅ |
| test_no_bias_when_doc_score_below_threshold | doc score 0.4 < 0.5 → LOW ostaje, LLM nije pozvan | ✅ |
| test_no_bias_when_no_extra_namespaces | extra_namespaces=None → LOW ostaje | ✅ |
| test_no_bias_when_doc_passages_empty | doc_passages=[] → LOW ostaje | ✅ |

**Napomena:** Fajl se sortira pre `test_doc_pitanje_api.py` (a < d) kako bi se importovao pravi `main` modul pre nego što API testovi postave mock. `patch.object(_real_main, ...)` osigurava da se patch primenjuje na pravi modul.

---

## 30Q Baseline

**Pre hotfixa:** 20✅/8⚠️/2❌  
**Posle hotfixa:** 20✅/8⚠️/2❌ ✓ (identično)

Gate bias se aktivira samo za `extra_namespaces` upite. Standardni 30Q pipeline koristi `extra_namespaces=None` — nema uticaja.

---

## Commit-ovi
1. `fc3050a` fix(p2-3-1): doc confidence gate bias in ask_agent
2. `0742f77` test(p2-3-1): 5 gate bias tests

---

## Smoke (manual verifikacija)

Upload `sample_ugovor.docx` → dobiti `session_id`  
POST `/api/dokument/pitanje` sa `session_id` i pitanjem o dokumentu  
Očekivano: `confidence != "LOW"`, odgovor pominje "vašeg dokumenta" ili "ugovora"
