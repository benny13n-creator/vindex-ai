# Vindex AI — Security Gap Register

**Date:** 2026-07-23
**Method:** 5 parallel adversarial code audits (auth/authz/multi-tenancy; AI security/file upload; API/frontend/secrets; infrastructure/supply chain/DR/monitoring; database/RLS/audit-integrity/GDPR) + direct review of `security/crypto.py`. Evidence-only — every row cites `file:line`. Where evidence could not be obtained from the repository, the row says `REQUIRES PRODUCTION VERIFICATION`.

Companion documents: `SECURITY_DUE_DILIGENCE_REPORT.md` (full narrative per domain, including the 2026-07-23 post-sprint score update), `SECURITY_ROADMAP.md` (P0–P3 sequencing), `PUBLIC_SECURITY_CLAIMS.md` (what can/cannot be said publicly), `EXECUTIVE_SECURITY_SUMMARY.md` (investor/technical-due-diligence-facing summary, honestly re-scored at 66/100 — see that document for why it is not higher yet).

**Status as of 2026-07-23 (P0–P5 sprints complete):** SEC-001, SEC-002 (message), SEC-003, SEC-005, SEC-007, SEC-008 (confirmed instance), SEC-009, SEC-023, and **SEC-031 (production-verified, not just deployed)** all closed or resolved as scoped. SEC-031's closure surfaced a new finding, SEC-034 (migrations that silently no-op against pre-existing incomplete tables) — its migration-source-level audit is now done and clean (zero additional instances found across all 69 migration files), but live verification against production is still pending founder action, so it remains open at P1. SEC-033 and most other P1/P2/P3 items remain open. See `EXECUTIVE_SECURITY_SUMMARY.md` for the full current-state summary and `SEC031_PRODUCTION_EXECUTION_LOG.md` for the full SEC-031 production execution record.

---

| ID | Problem | Severity | Likelihood | Impact | Evidence | Recommended Fix | Est. Effort | Priority |
|---|---|---|---|---|---|---|---|---|
| SEC-001 | `POST /api/predmeti/{id}/beleske` and `.../istorija` insert without verifying `predmet_id` belongs to the caller — confirmed cross-tenant write, visible on victim's next read | **CRITICAL** | High (any authenticated user, no special access needed) | High — arbitrary data injection into another firm's case file | `api.py:3240-3253`, `api.py:3264-3276`; contrast correct pattern at `api.py:3806-3838` (upload) and `api.py:3161` (GET) in the same file | **CLOSED (2026-07-23).** Ownership check added to both endpoints, identical pattern to `get_predmet`/`update_predmet` in the same file. **Full sweep completed** (not just these 2 instances): all 24 `{predmet_id}`-scoped mutation endpoints across `api.py` and every router file were checked — these were the only two missing the check; the other 22 already use one of three existing correct patterns (inline `.eq("id",...).eq("user_id",...)`, a named helper like `_dohvati_predmet`/`_proveri_vlasnistvo`, or a `PermissionService`-gated route with an inline check). No sibling instances found. 6 regression tests added (`tests/test_sec001_predmet_ownership.py`): cross-tenant write rejected (both endpoints), owner's legitimate write still succeeds (both endpoints), founder status does not bypass the check, non-existent predmet_id rejected identically to someone-else's (no existence-vs-ownership oracle). Full suite: 1725 passed. | Small (hours, isolated) | **P0 — DONE** |
| SEC-002 | GDPR account-erasure endpoint anonymizes only `profiles` + `korisnik_email_notif`; response text claimed "predmeti i dokumenti ostaju u sistemu u anonimizovanom obliku" but no code path touches `predmeti`/`klijenti`/`predmet_dokumenti`/`case_dna`/`billing_entries` | **CRITICAL → downgraded to MEDIUM (message-only fix, 2026-07-23)** | Certain (every erasure request) | Was: materially false compliance claim. Now: message is accurate; the underlying retention-policy question (should case data ever be deleted/anonymized) remains open pending a formal Advokatska Data Retention Politika — explicit founder decision, not a code question | `routers/gdpr.py:222-227` (corrected message); `docs/security/SEC002_DATA_RETENTION_ANALYSIS.md` §4 (the fix design); `docs/security/P0_FIX_VERIFICATION.md` (proof) | **MESSAGE FIX DONE (2026-07-23).** Endpoint behavior unchanged (still anonymizes only `profiles`); response text now accurately states `predmeti`/`klijenti`/`dokumenti` are retained per the lawyer's statutory duty, not anonymized. Explicitly NOT a cascade-delete/anonymize implementation — founder decision to keep it message-only, pending the retention-policy decision. Regression-guarded by `tests/test_gdpr_delete.py` (5 tests, fails loudly if predmeti/klijenti/klijent_dokumenti/predmet_dokumenti are ever touched by this endpoint). | Small (message only) | **P0 — message fixed, data-handling question remains open by design** |
| SEC-003 | Prompt-injection defense (`security/prompt_guard.py`) was applied at exactly one call site (`POST /api/pitanje`); `wrap_for_ai()`, the strongest layer, was called nowhere. Case Genome extraction, Evidence classification, and ~130 other GPT call sites (53 files — original audit estimated "~50+", exact census found 130) fed user/document-supplied text directly into prompts with zero screening | **CRITICAL → CLOSED (2026-07-23)** | Was: Medium-High | Was: Medium-High — indirect injection could manipulate Genome/Evidence extraction output | `shared/ai_client.py::_patch_prompt_guard()` (the fix); `security/prompt_guard.py::PromptInjectionBlocked` (new exception type); `api.py:26-28` (bootstrap wiring); `tests/test_sec003_llm_wrapper.py` (12 tests); `docs/security/P0_FIX_VERIFICATION.md` (proof) | **CLOSED (2026-07-23).** Central guard patches `openai.resources.chat.completions.completions.Completions.create`/`AsyncCompletions.create` at the **class** level (same technique `_patch_openai_module()` already used for Azure redirect) — structurally intercepts every one of the 130 call sites regardless of how/where the client is constructed, with zero per-call-site code changes. `analyze()` runs on 'user'-role message content (string or multimodal content-parts) before every call; if blocked, `PromptInjectionBlocked` is raised and the OpenAI call never executes. Global FastAPI exception handler added as a fallback for call sites that don't explicitly catch the exception. **Known, deliberate scope limit**: this closes the *blocking* gap fully; it does NOT auto-inject `wrap_for_ai()`'s message-isolation framing into the other 129 call sites' existing message content (assessed as higher regression risk — would require per-call-site verification of message shape — not attempted). Full suite: 1737 passed (1725 pre-existing + 12 new). | Small–Medium (one central patch, not 130 individual edits) | **P0 — DONE** |
| SEC-004 | `SUPABASE_SERVICE_KEY` (used by the one app-wide Supabase client) bypasses RLS entirely — tenant isolation for ~150+ endpoints rests 100% on each handler manually filtering by `user_id`; RLS is not a backstop for a missed filter (see SEC-001) | **CRITICAL** (architectural) | N/A — this is a standing architectural fact, not a triggerable event | Total — a leaked key or any missed-filter endpoint has unrestricted blast radius | `shared/deps.py:29,72-80` | Not fixable by "adding a check" — this is a design-level fact to manage via defense-in-depth: (1) close SEC-001-class gaps, (2) add automated testing that asserts every mutating predmet/klijent/dokument route filters by `user_id`, (3) treat `SUPABASE_SERVICE_KEY` rotation and access-logging as P0-adjacent infra work | Large (process + tooling, not one patch) | **P0 (mitigate), ongoing** |
| SEC-005 | Rate limiter (`shared/rate.py`) and behavioral anomaly detection (`security/anomaly_detection.py`) both use in-process memory with 4 gunicorn workers (`WEB_CONCURRENCY=4`) and no shared backing store — real effective rate limit is up to ~4× the stated value; anomaly baselines see only ~1/4 of real per-user traffic | HIGH | High (happens under normal load-balanced traffic, no attacker sophistication needed) | Medium — weakens (does not eliminate) both cost-control and abuse-detection | `shared/rate.py` (pre-fix), `api.py:514-519` (pre-fix, documented the prior incident that forced in-memory-only), `security/anomaly_detection.py:50-53` (still open, see note) | **CLOSED for the rate limiter (2026-07-23); anomaly detection's own Redis wiring is a separate, smaller, not-yet-scoped follow-on.** `shared/rate.py::build_limiter()` wires `storage_uri` to Redis (when `REDIS_URL` is set) with `in_memory_fallback_enabled=True` + `swallow_errors=True` — slowapi's own built-in mechanism (confirmed by reading its source: `_check_request_limit` already wraps the whole check in one `except Exception`), not a custom wrapper. Tested specifically against the exact original incident (`redis.ResponseError` from Upstash quota exhaustion, not just a generic connection failure) — 6 regression tests in `tests/test_sec005_failopen_limiter.py`. Both of this codebase's two independent `Limiter` instances (`shared/rate.py` and `api.py`'s own — a pre-existing duplication, noted but not consolidated) now share the same factory. Also closed 3 specific critical-route gaps found during this review: `routers/case_dna.py` (`refresh_case_dna`, `compare_docs`) and `routers/legal_reasoning.py` (`reasoning_graph_generate`) had zero rate-limit decorators despite being real GPT-generation endpoints. **Disclosed, not verified from this repo**: whether `REDIS_URL` is actually set in production — the capability is built and tested; confirming live activation is an infrastructure fact outside this repository's visibility. `security/anomaly_detection.py`'s in-memory-per-worker limitation was not touched this cycle. | Medium | **P1 — DONE (rate limiter); anomaly detection follow-on still open** |
| SEC-006 | PII scrubbing (`main.py::_skini_pii`) masks only numeric identifiers (JMBG/PIB/MB/ID/passport/phone/IBAN) — person names and addresses are never masked, and the function is not called at all on the Case Genome extraction path, the single richest source of party PII | HIGH | Certain (every Genome extraction sends full names/addresses to OpenAI) | Medium-High — real third-party-processor PII exposure in a legal-case product | `main.py:1003-1018` (masking set), grep confirms 4 call sites total, `routers/case_dna.py` absent from that list | Do not claim "PII masked before AI processing" without qualifying scope. Extending coverage to names/addresses needs NER, not regex — scope as a real P1/P2 project, not a quick patch | Medium (state the limit) / Large (fix properly) | **P1 (disclose now, fix scoped separately)** |
| SEC-007 | ~~DOCX parsing has no decompressed-size or entry-count ceiling~~ **CLOSED (2026-07-23).** `uploaded_doc/extractor.py::_check_docx_zip_safety()` inspects the ZIP central directory (metadata only, no decompression) before `python-docx` ever touches the file — rejects if total declared decompressed size > 50MB, any single entry's ratio > 100:1, or entry count > 2000. Also added a PDF page-count cap (500 pages, SEC-027 companion) since `extract_pdf` shares this file. Fixed centrally (one function, `uploaded_doc/extractor.py`), protecting all 6 call sites (`api.py`, `routers/dokument.py`, `routers/drafting.py`, `routers/smart_intake.py`→`shared/intake_worker.py`) without touching each individually — same "fix at the shared chokepoint" pattern used for SEC-003. `api.py` and `routers/dokument.py`/`routers/drafting.py` upload endpoints now return a clean `413` on rejection; `shared/intake_worker.py`'s background queue already had generic per-job exception handling that fails the job gracefully. 5 regression tests in `tests/test_security_p1_p2.py` (realistic high-ratio fixture using genuine DEFLATE compression, not synthetic metadata). **Deviation from the task's illustrative 10:1 ratio example, stated explicitly**: used 100:1 instead — ordinary legal-document XML compresses well past 10:1 legitimately (repetitive boilerplate/table markup), so a literal 10:1 cap would reject real documents; the 50MB absolute cap is the primary, unambiguous defense regardless of ratio threshold choice. | `uploaded_doc/extractor.py` | See above | Small | **P1 — DONE** |
| SEC-008 | Confirmed unescaped `innerHTML` injection in the court-portal case-tracking widget (client-side, `static/vindex.js`), including a field (`p.last_error`) that commonly echoes external/attacker-influenced content | HIGH → **instance CLOSED (prior session, P0)**; **server-side defense-in-depth ADDED (2026-07-23)** | Medium (requires the case-tracking widget to render attacker-influenced data) | Medium-High — session-token theft plausible given Supabase's default localStorage token persistence | `static/vindex.js:21735-21748` (client-side fix, prior session); `routers/portal_monitoring.py` (server-side sanitization, this session) | Client-side: fixed with `escHtml()` (prior session). **This session, additionally**: `security/html_sanitize.py::sanitize_text()` (bleach-based, strips all tags/attributes) applied at the actual data source — `routers/portal_monitoring.py::_scrape_portal_status()`'s `status` field and `_current_status_update()`'s `last_error` field, both of which originate from scraping the external `portal.sud.rs` court portal — so the stored/returned value is already clean even if a future frontend change forgets `escHtml()`. **The full ~418-site client-side `.innerHTML=` sweep is explicitly NOT done** — still tracked as its own, separate P1 item below, not silently folded into this session's narrower "court-portal widgets, document preview, analysis view" scope | `security/html_sanitize.py`, `routers/portal_monitoring.py` | Small (this session's defense-in-depth) / Medium (full sweep, still open) | **P0 (instance) — DONE; P1 (full sweep) — still open, see below** |
| SEC-009 | ~~Bulk CSV/XLSX client import never calls `encrypt_field()`~~ **CLOSED (2026-07-23).** Confirmed the exact outcome (previously marked `REQUIRES PRODUCTION VERIFICATION`): the old code wrote directly to `"pib"`, a column that does not exist in the `klijenti` schema at all (only `pib_encrypted` does) — every CSV row containing a PIB value would fail as part of its 25-row batch insert, a silent functional failure, not a plaintext-write. Fixed to route through the same `encrypt_field()` primitive (AES-256-GCM) the manual-entry path already uses, targeting `pib_encrypted` — same ciphertext format (`enc_v1:...`), same decrypt-on-authorized-view path already correct in `klijenti/router.py` (no read-side changes needed). 4 regression tests (`tests/test_sec009_pii_encryption.py`): plaintext `pib` key never appears in insert data, ciphertext round-trips correctly, rows without PIB are unaffected, bulk and manual paths produce structurally identical ciphertext. | HIGH | Medium (requires using the bulk-import feature with PIB data present) | Medium — confirmed functional failure, not plaintext exposure (see above) | `routers/import_klijenti.py:198-211` (fix); contrast `klijenti/router.py:220-225` (the pattern it now matches) | Done — see above | Small | **P0 — DONE** |
| SEC-010 | Rate limiting coverage is inconsistent — `klijenti/router.py` (0/20 routes), `case_dna.py`/`health_index.py`/`intelligence_timeline.py`/`legal_reasoning.py` (0/12 routes combined) have zero `@limiter.limit()` decorators, including the new Legal Reasoning Engine's GPT-4o generation endpoint (real cost per call, unmetered) | HIGH | High (no special access needed, just repeated calls) | Medium — direct AI-cost abuse vector plus the underlying `default_limits` may be non-enforcing app-wide (see SEC-011) | `shared/rate.py:20`, decorator count 397 across 569 total routes (~30% undecorated); specific files confirmed at 0 coverage | Add `@limiter.limit()` to every AI-cost-bearing and PII-touching route at minimum; prioritize `legal_reasoning.py` and `klijenti/router.py` | Small | **P1** |
| SEC-011 | `SlowAPIMiddleware` is never registered — `default_limits=["60/hour"]` may be non-enforcing for any route without an explicit `@limiter.limit()` decorator (slowapi behavior, NOT VERIFIED empirically against this specific deployment) | MEDIUM-HIGH | Unknown pending verification | Medium — if true, SEC-010's ~172 undecorated routes have effectively zero rate limiting, not a relaxed default | `shared/rate.py:20`, `api.py:525,535` — no `SlowAPIMiddleware` found anywhere in the codebase | Add `app.add_middleware(SlowAPIMiddleware)` (one line) so `default_limits` becomes a real floor | Trivial | **P0 (cheap, high-value)** |
| SEC-012 | Citation/legal-source verification (`shared/genome_validator.py`) checks only whether a cited article number is numerically plausible, not whether it actually exists in the retrieved corpus — GPT can cite a wrong-but-plausible article and pass verification | MEDIUM-HIGH | High (inherent to how GPT-4o generates citations) | Medium — legal-hallucination risk in a legal-advice-adjacent product | `shared/genome_validator.py:119-160`, own docstring admits scope limit at line 138 | Do not publicly claim "citation verification" for the live Genome pipeline. The new `services/legal_reasoning_engine.py` (Phase 0, unshipped) already does this correctly — prioritize wiring it into the primary pipeline over building a second solution | Large (already partially built, needs integration) | **P2** |
| SEC-013 | Event dispatch loop (`services/event_bus.py`) has no atomic row-claim (`FOR UPDATE SKIP LOCKED`) despite running independently in all 4 gunicorn workers — two workers can select and dispatch the same event, causing duplicate GPT calls/audit entries | MEDIUM | Medium (requires specific timing under concurrent load) | Medium — duplicate side effects, not data loss or leakage | `services/event_bus.py:274-330`, contrast correct pattern already in `shared/intake_worker.py:41` (same codebase, different loop) | Apply the same `FOR UPDATE SKIP LOCKED` RPC pattern already used by `intake_worker.py` to the event dispatch loop | Small–Medium | **P1** |
| SEC-014 | CSP sets `script-src 'unsafe-inline'` and `style-src 'unsafe-inline'`, driven by the frontend's extensive use of inline `onclick="..."` handlers — materially weakens CSP as an XSS mitigation despite CSP being "present" | MEDIUM | N/A (structural) | Medium — reduces defense-in-depth against SEC-008-class findings | `api.py:974-989` (CSP config), `static/vindex.js` (extensive inline handler usage, e.g. lines 21746-21747) | Not a quick fix — migrating all `onclick=` to `addEventListener` is a large frontend refactor. Accept as a known, documented tradeoff for now rather than claim strong CSP protection | Large | **P2** |
| SEC-015 | File-type validation trusts the client-supplied `Content-Type` header, not actual file content (magic bytes) — mitigated in practice by unhandled parse exceptions on mismatched content (500, not a silent bypass) | MEDIUM | Low (limited practical exploitability found) | Low-Medium — robustness/DoS-adjacent, not confirmed content-type confusion | `api.py:3844` | Validate magic bytes before parsing; wrap `extract()` in try/except returning clean 422 instead of unhandled 500 | Small | **P2** |
| SEC-016 | No malware/AV scanning on uploaded files — mitigated by extraction-only usage pattern (text is parsed out, not re-served as a binary), but not eliminated as a risk class | MEDIUM | Low (bounded by extraction-only usage, NOT VERIFIED whether originals are ever re-served for download) | Low-Medium | No scanning call found anywhere in the upload path | Confirm originals are never re-served as downloadable binaries; if they are, add AV scanning | Medium | **P2** |
| SEC-017 | No login success/failure audit trail — `log_action("login_success"/"login_failed")` defined in `AUDITABLE_ACTIONS` but never called anywhere; likely because auth happens client-side via Supabase directly, backend never observes it | MEDIUM | N/A (standing gap) | Medium — cannot detect credential-stuffing/brute-force from this system's own audit log | `shared/audit_immutable.py:41` (defined), zero call sites confirmed by full-repo search | Either wire a client-side call to a backend logging endpoint on auth state change, or explicitly document that login monitoring is delegated to Supabase Auth's own dashboard (REQUIRES PRODUCTION VERIFICATION whether Supabase provides this) | Small–Medium | **P1** |
| SEC-018 | Legacy plaintext `jmbg_mb` column was never dropped after migrating to `jmbg_encrypted` — commented-out `DROP COLUMN` found in schema history | MEDIUM | N/A pending verification | Medium — possible live plaintext JMBG data, REQUIRES PRODUCTION VERIFICATION | `supabase_setup.sql:67` | Verify column contents in production; drop if empty, migrate-then-drop if not | Small | **P1** |
| SEC-019 | Two tables (`zakoni_monitoring`, `case_benchmarks`) have no RLS policy, inconsistent with every sibling table in the same migration file. Mitigated by `GRANT`s restricted to `service_role` only (no `authenticated`/`anon` grant found) | MEDIUM (mitigated) | Low given current GRANT configuration | Low — defense-in-depth gap, not directly reachable via REST API under current grants | `migrations/045_firm_intelligence.sql:92,190,108,208` | Add `ENABLE ROW LEVEL SECURITY` + a `USING (false)` policy for defense-in-depth, matching the `audit_immutable` pattern | Trivial | **P2** |
| SEC-020 | No dependency lock file (`poetry.lock`/equivalent absent) — 16 of 33 `requirements.txt` entries are loose-pinned (`>=`), including `cryptography`, `argon2-cffi`, `sentry-sdk` | MEDIUM | Medium (any fresh install/rebuild) | Medium — uncontrolled version drift on security-critical libraries | `requirements.txt` (17 exact, 16 loose) | Pin exact versions for all packages, especially `cryptography`/`argon2-cffi`; add a lock file to the build process | Small | **P1** |
| SEC-021 | `/health` endpoint returns a static OK with no dependency check (DB/Pinecone/OpenAI) — hosting platform can report healthy while Supabase is fully unreachable | LOW-MEDIUM | N/A (standing gap) | Medium during an actual outage — delayed incident detection | `api.py:1375-1384`, `railway.toml` healthcheckPath | Add a lightweight DB ping to the health check | Small | **P2** |
| SEC-022 | JWT verification sets `verify_aud: False` in both local-decode paths — audience claim never checked | MEDIUM | Low (requires key/secret reuse across environments/projects) | Medium if that precondition ever holds | `shared/deps.py:190,207` | Set an explicit expected audience and verify it, once the correct value is confirmed for this Supabase project | Small | **P2** |
| SEC-023 | ~~`hash_password`/`verify_password`/`needs_rehash` (Argon2id) are fully implemented but have zero call sites~~ **CLOSED (2026-07-23).** Docstring corrected — no longer claims this is the "HARD RULE" for how login passwords work; explicitly states real user auth is delegated to Supabase Auth, and these functions remain available as a tested, correct Argon2id primitive for a future local-hashing need (e.g. long-lived API/integration tokens) outside the Supabase Auth login flow. Zero call sites re-confirmed (grep, still true) — labeling fix only, no behavior change, no new call site invented | `security/crypto.py:7-24, 215-220` | See above | Trivial (docs) | **P1 — DONE** |
| SEC-024 | Encryption key rotation is not implemented — `KEY_ID` format exists in ciphertext values (`enc_v1:k{id}:...`) but `_get_key_by_id()` for multi-key lookup is a stub; only one active key supported | MEDIUM | N/A (standing gap) | Medium — no ability to rotate `FIELD_ENCRYPTION_KEY` without a full re-encryption event | `security/crypto.py:178-184` (comment: "Faza 10b" stub) | Implement multi-key lookup keyed by `KEY_ID`, enabling gradual rotation | Medium | **P2** |
| SEC-025 | DR runbook (`scripts/dr_runbook.py`) references "Render.com dashboard" throughout, while `railway.toml` and `gunicorn.conf.py` comments suggest Railway is the actual host — REQUIRES PRODUCTION VERIFICATION which is current | LOW-MEDIUM | N/A | Medium during an actual incident if stale | `scripts/dr_runbook.py:24,31,36,40` vs. `railway.toml` | Confirm actual live host and correct the runbook if stale — a wrong runbook during a real incident is worse than no runbook | Trivial | **P1 (cheap, high incident-response value)** |
| SEC-026 | Hardcoded JWKS fallback public key in source — stale-key denial-of-auth risk (legitimate tokens rejected) if Supabase rotates its signing key and live JWKS fetch fails past the 1h cache | LOW-MEDIUM | Low | Low (denial, not bypass) | `shared/deps.py:113-118` | Move fallback key to env var or remove the fallback (fail loudly instead of silently trusting stale key material) | Small | **P2** |
| SEC-027 | PDF/OCR has no page-count cap — partially mitigated by 10MB size cap and 10/minute rate limit | LOW | Low | Low | `uploaded_doc/extractor.py:9-27,57-60` | Add a page-count ceiling as defense-in-depth | Small | **P3** |
| SEC-028 | `.doc` (legacy Word) accepted in `_ALLOWED_SUFFIXES` but unhandled in the extractor — produces an unhandled 500 rather than a clean error | LOW | Low | Low (robustness, not a vuln) | `api.py:3803` vs. `uploaded_doc/extractor.py:120-128` | Either add a `.doc` handler or remove it from allowed suffixes | Trivial | **P3** |
| SEC-029 | No intra-tenant RBAC (role hierarchy within a single firm/account) — may be an intentional current-scope limitation given the product's single-user-per-account pattern to date | INFO | N/A | N/A pending product-intent confirmation | No role-hierarchy code found within a `predmet_id` scope | Confirm against product intent (multi-attorney firms) rather than treat as a bug by default | N/A | **Backlog — confirm intent first** |
| SEC-030 | `api_key_rotation` audit action defined but never called — key rotation, if it ever happens, is entirely manual and untracked | LOW | N/A | Low | `shared/audit_immutable.py:55`, zero call sites | Wire a log call into whatever manual key-rotation runbook exists (or write one) | Small | **P3** |
| SEC-031 | ~~`ON DELETE CASCADE` from `auth.users(id)` wired through dozens of tables — any direct deletion of a user at the Supabase Auth layer bypasses the app and irrecoverably destroys every case, client, and financial record tied to that user~~ **CLOSED (2026-07-23) — CONFIRMED IN PRODUCTION, not just deployed.** `migrations/077_sec031_restrict_auth_users_cascade.sql` (v4) executed against production by the founder; all 18 Tier A `auth.users`-referencing FK constraints verified `ON DELETE RESTRICT` via direct read-only query against `pg_constraint` after execution — not assumed, not inferred from "the script ran." Two genuine mid-execution failures occurred and were resolved cleanly (see `docs/security/SEC031_PRODUCTION_EXECUTION_LOG.md` for the full step-by-step record): GRUPA 1 failed once on `predmet_delegiranja` (table existed in bare/incomplete form, its own migration 054 had silently no-op'd via `CREATE TABLE IF NOT EXISTS`), GRUPA 3 failed once on `tos_acceptances` (identical pattern, migration 056). Both transactions rolled back cleanly with zero data touched on failure — exactly the designed safety property, now proven in practice, not just in tests. Both underlying migrations (054, 056) were run, the two missing FKs added manually to match, and both groups re-run successfully. Direct `DELETE FROM auth.users` can no longer cascade-destroy case, client, or financial data in production — confirmed, not theoretical. | `supabase_setup.sql:281-332`, `migrations/002_klijenti_crm.sql`, `migrations/003_billing.sql:1-45`; full graph and proof: `docs/security/SEC031_FK_GRAPH.md`; execution record: `docs/security/SEC031_PRODUCTION_EXECUTION_LOG.md` | Done — see above | Design+code+production execution: all done | **P0 — DONE, PRODUCTION-VERIFIED** |
| SEC-034 | `CREATE TABLE IF NOT EXISTS` migrations can silently no-op with zero error if the target table already exists in an incomplete/bare form — discovered directly during SEC-031's production execution: both `predmet_delegiranja` (migration 054) and `tos_acceptances` (migration 056) existed in production with only their columns and a bare `PRIMARY KEY`, missing every FK, RLS policy, and index their migration defines, with no error signal that anything was skipped. `tos_acceptances`'s absence meant `GET /api/tos/status` fail-opened to `accepted=true` on every DB error — no user had ever had a valid, recorded ToS/AI-consent acceptance in production until migration 056 was actually run today | HIGH | Static migration-source audit complete (2026-07-23): parsed all 69 files in `migrations/`, found 127 tables enabling RLS; an initial regex census flagged 15 as "RLS enabled, no policy" — reading each by hand showed 11 were false positives (multi-line/`DO $$` policy syntax the first regex missed) and the remaining 4 (`email_notif_log`, `kancelarija_clanovi`, `law_docs`, `security_events`) are intentional, documented service-role-only tables, not gaps. **Zero additional SEC-034-pattern instances found in the migration source.** This is real progress but does NOT close the finding: a well-written migration file (054 and 056 both were) is exactly what already failed silently against live state twice — the risk is invisible to source-only analysis by construction. `tests/test_sec034_migration_completeness.py` now locks in this source-level result (and will fail if a future RLS-enabled table is added with no policy and no allowlist justification), but the actual live-vs-source comparison for all 127 RLS tables is still `REQUIRES PRODUCTION VERIFICATION` — not yet run | High — a migration file can be "successfully run" and still leave the schema (and, in `tos_acceptances`'s case, a live compliance control) completely unprotected, invisibly | `docs/security/SEC031_PRODUCTION_EXECUTION_LOG.md` (full incident record); `migrations/054_predmet_delegiranja.sql`, `migrations/056_tos_acceptances.sql`; `tests/test_sec034_migration_completeness.py` (static audit, 7 tests, all passing); `scripts/sec034_live_completeness_check.sql` (read-only, single-pass live diagnostic covering all `public` tables — RLS flag, FK/policy/index counts — not yet run against production) | Founder runs `scripts/sec034_live_completeness_check.sql` once against production; anything it flags gets triaged table-by-table (most will be legitimate, same as this static pass — but this is the step that actually answers "are there more `predmet_delegiranja`-shaped tables"). Separately, still open regardless of that script's result: confirm whether `predmet_delegiranja`'s `predmet_id→predmeti` FK/RLS/policies/indexes and `tos_acceptances`'s RLS/policies actually landed when migration 054/056 ran today — this was assumed missing in the original incident log but never directly re-queried after the run, so it is an open question, not a known gap (see `routers/enterprise.py` review below: both delegation endpoints are structurally correct against migration 054's schema, but their live behavior is unverified from this environment) | Live script: none (read-only). Triage after: Small–Medium depending on findings | **P1 — static audit done and clean; live verification still pending founder action** |
| SEC-032 | PII encryption is not uniform across tables holding the same identifier — `klijenti.pib_encrypted` is AES-256-GCM encrypted, but `fakture.klijent_pib` (same PIB value, copied onto the invoice) is stored in plaintext | LOW-MEDIUM | Certain (every invoice with a PIB) | Low-Medium — inconsistent protection of the same sensitive identifier depending on which table it's read from; smaller blast radius than SEC-031 but same root cause class (no single source of truth for "how is this field protected") | `migrations/003_billing.sql` (`fakture.klijent_pib`, plaintext column) vs. `migrations/002_klijenti_crm.sql` (`klijenti.pib_encrypted`) | Not urgent — noted for a future PII field registry (a per-field policy: encrypted? hashed? searchable? retention? export behavior?) rather than a one-off patch to `fakture` alone, since the same inconsistency likely recurs elsewhere once a registry is actually built | Small (this instance) / Medium (registry, if built) | **P2 — bundle into a future PII field registry, not urgent standalone** |
| SEC-033 | `klijenti.user_id` has **no foreign key constraint at all** to `auth.users` (`TEXT NOT NULL`, no `REFERENCES`). **Confirmed while building `SEC031_FK_GRAPH.md` that this is not an isolated case**: the identical pattern (an owner/creator column with zero FK enforcement) was independently verified by direct read of the `CREATE TABLE` source across at least 8 more tables in 4 unrelated feature areas: `kancelarije.admin_uid`, `kancelarija_clanovi.user_id`, `zadaci.kreirao_uid`/`dodeljen_uid`/`predmet_id` (firm/workflow subtree); `style_profili.user_id`, `style_analize.user_id`, `knowledge_profiles.user_id`, `knowledge_upiti.user_id` (org intelligence subtree); `recommendation_log.user_id` (learning loop); `intake_jobs.uploaded_by`/`predmet_id` (intake pipeline, `predmet_id` additionally typed `TEXT` instead of `UUID`). This looks like a recurring pattern from how new features were built over time, not a single oversight | LOW-MEDIUM (per-instance) / MEDIUM (as a systemic pattern) | Unknown — requires checking for orphaned rows in production, `REQUIRES PRODUCTION VERIFICATION` | Low-Medium per table (data-integrity gap, not an access or destruction risk on its own) — but the *pattern* recurring across 4 independently-built feature areas suggests a process gap (missing FK is not caught in review) worth addressing structurally, not table by table | `supabase_setup.sql:570` (`klijenti.user_id`); `migrations/018_kancelarija.sql:10`; `migrations/045_firm_intelligence.sql:112-117`; `migrations/040_faza5_org_intelligence.sql:12,26,44,64`; `migrations/037_learning_loop.sql:44`; `migrations/073_intake_foundations.sql:76-78` | Recommend scoping this as its own **Integrity Audit initiative** (per founder's own framing) rather than a single migration: systematically enumerate every owner/creator/reference column schema-wide, check for missing FKs, `TEXT`-vs-`UUID` type mismatches, nullable FKs that should be required, and actual orphaned-row counts in production — then design one coherent remediation, not N one-off patches | Medium (audit) + Small–Medium per fix once scoped | **P2 — scope as a dedicated Integrity Audit, not bundled into SEC-031, not started yet** |

---

**Severity legend:** CRITICAL = live/exploitable or legally material today. HIGH = real, credible exploit path or material exposure, not yet confirmed exploited. MEDIUM = real gap, meaningfully mitigated by another control or requiring specific conditions. LOW = robustness/best-practice gap with limited standalone impact. INFO = no action implied, noted for completeness or requires a product decision, not a security fix.
