# Testovi koji dodiruju PRODUKCIONU Supabase bazu umesto da mokuju klijent.
#
# LISTA JE PRAZNA. To je ciljno stanje, ne pokvaren fajl.
#
# ISTORIJA, da se ne izgubi zasto fajl uopste postoji.
# Wave 9 (2026-08-11) je izmerio da 115 testova iz ~40 fajlova dodiruje
# produkcionu bazu, i zamrznuo ih ovde kao "SADRZANO, NE ZATVORENO". Uzrok je
# bio to sto `tests/conftest.py` ucitava `.env` (zbog `FOUNDER_EMAILS` i
# `FIELD_ENCRYPTION_KEY`), pa su zivi `SUPABASE_URL` i `SUPABASE_SERVICE_KEY`
# ulazili u test proces i `_get_supa()` je vracao PRAV klijent ka produkciji.
#
# Wave 10 (2026-08-11) je problem resio na nivou KONFIGURACIJE umesto na nivou
# DNS-a: `.env` DB kredencijali se vise ne uvoze u test proces, a izricito
# izvezena produkciona konfiguracija obara suite pre prvog testa
# (`tests/prod_db_guard.py`).
#
# MERENJE POSLE TE PROMENE: od 455 testova u tih 42 fajla pada TACNO JEDAN --
# `tests/test_ztc_scenario_b_attach.py::test_finalize_without_predmet_id_still_creates_new_case`,
# koji je kroz `routers/smart_intake.py:1525` -> `audit_immutable.log_action`
# -> `_get_last_hash(supa)` stvarno citao i pisao u produkcioni hash-lancani
# ledger. On je popravljen (mokovan `audit_immutable`), i lista je prazna.
#
# Ostalih 114 je produkciju dodirivalo iskljucivo kroz fail-soft putanje koje se
# identicno ponasaju i kad hosta nema. Allowlist im dakle nikad nije ni trebao --
# bio je posledica toga sto se problem resavao na pogresnom sloju.
#
# ZASTO FAJL OSTAJE
# On je allowlist brane iz `tests/conftest.py`. Prazan dokumentuje pravilo i daje
# buducem prestupniku mesto da bude zabelezen NAMERNO, umesto da neko ugasi branu.
# `tests/test_prod_db_offenders_baseline.py` fiksira maksimum na 0, pa dodavanje
# imena obara suite dok neko svesno ne podigne broj, uz obrazlozenje.
#
# STA BI KOSTALO DA IME STOJI OVDE
# Svako pokretanje suite-a -- lokalno i u CI-ju -- citalo bi i pisalo u
# produkcionu bazu. Za `audit_immutable` i `ai_provenance` je nepovratno: one su
# append-only iza BEFORE UPDATE/DELETE trigera i hash-lancane, pa se upisan red
# NE MOZE obrisati.
